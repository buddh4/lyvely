import { INotificationChannel } from '@/notifications/interfaces';
import { User } from '@/users';

/*export class LogNotificationChannel implements INotificationChannel {
  getId(): string {
    return LogNotificationChannel.name;
  }

  send(user: User, notification: Notification) {}

  sendBulk(user: User[], notification: Notification) {}
}*/
