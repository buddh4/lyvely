import { createContentFeature } from '@lyvely/interface';
import { JOURNALS_MODULE_ID } from './journals.constants';

export const JournalsFeature = createContentFeature('journals', JOURNALS_MODULE_ID);
